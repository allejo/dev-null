name: Demo Workflow

on:
  issues:
    types: [labeled]

jobs:
  notify-slack:
    runs-on: ubuntu-latest
    if: github.event.label.name == 'demo-1' || github.event.label.name == 'demo-2'
    steps:
        - run: |
            echo "hello"
